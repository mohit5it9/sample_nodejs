language: node_js

node_js:
  #- 0.12
  - 5.0.0
service:
  - mysql
env:
  - XUNIT_FILE=shippable/testresults/result.xml

build:
  ci:
    #- npm install
    #- npm test
    #- npm test1
    - echo "hello"
    - echo "test some new commands"
    - echo ls
